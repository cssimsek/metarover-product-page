language: node_js
node_js:
  - "6"
before-install:
  - cd functions
install:
  - npm init -f
  - npm install
script:
  - firebase deploy --token $FIREBASE_TOKEN
after_success:
  - echo "Deploy to Firebase succeeded"